import React from 'react';

type HomePageProps = {
  // ... свойства компонента
};

const HomePage: React.FC<HomePageProps> = () => {
  return (
    <div>

    </div>
  );
};

export default HomePage;